{% extends 'base.html.twig' %}

{% block title %}Stream{% endblock %}

{% block body %}
    <h1>Stream</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stream.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ stream.title }}</td>
            </tr>
            <tr>
                <th>Viewers</th>
                <td>{{ stream.viewers }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ stream.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_stream_index') }}">back to list</a>

    <a href="{{ path('admin_stream_edit', {'id': stream.id}) }}">edit</a>

    {{ include('admin/stream/_delete_form.html.twig') }}
{% endblock %}
